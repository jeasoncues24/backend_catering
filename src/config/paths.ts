import path from "path";

export const UPLOADS_PATH = path.join(__dirname, "..", "uploads");

export const UPLOADS_LOGOS_PATH = path.join(UPLOADS_PATH, "logos");
export const UPLOADS_BANNER_PATH = path.join(UPLOADS_PATH, "banners");
export const UPLOADS_SERVICES_PATH = path.join(UPLOADS_PATH, "services");
export const UPLOADS_BILLING_PATH = path.join(UPLOADS_PATH, "billing");
export const UPLOADS_PRODUCTS_PATH = path.join(UPLOADS_PATH, "products");